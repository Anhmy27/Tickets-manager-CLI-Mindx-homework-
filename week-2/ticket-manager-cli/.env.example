# Copy to .env and edit. CLI loads .env from this package folder.
# Shell variables override values in this file.

# mock | http
KB_CLIENT_MODE=mock

# Required when KB_CLIENT_MODE=http
KB_API_BASE_URL=http://127.0.0.1:4100
